server.port: 10000
spring:
  application:
    name: mybatis
  datasource:
    username: root
    password: cao981127
    url: jdbc:mysql:///fund?serverTimezone=UTC&useUnicode=true&characterEncoding=utf8&useSSL=false&allowPublicKeyRetrieval=true
    driver-class-name: com.mysql.cj.jdbc.Driver
